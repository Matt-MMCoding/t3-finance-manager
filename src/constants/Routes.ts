export enum DynamicRouteKey {
  UserDashboard = "[userid]",
}

export enum Routes {
  Home = "/",
  Login = "/login",
  Logout = "logout",
  Dashboard = "/dashboard/[userid]",
  HelpCenter = "/help-center",
}
